<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.crysknife.tests</groupId>
        <artifactId>j2cl-tests-rest-parent</artifactId>
        <version>0.10</version>
    </parent>

    <artifactId>j2cl-tests-rest-client</artifactId>
    <name>Crysknife :: Tests :: J2CL :: REST :: Client</name>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>j2cl-tests-rest-shared</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.crysknife</groupId>
            <artifactId>crysknife-core</artifactId>
            <version>${crysknife.version}</version>
        </dependency>
        <dependency>
            <groupId>io.crysknife.ui</groupId>
            <artifactId>crysknife-ui-core</artifactId>
            <version>${crysknife.version}</version>
        </dependency>
        <dependency>
            <groupId>io.crysknife.ui</groupId>
            <artifactId>crysknife-ui-rest-api</artifactId>
            <version>${crysknife.version}</version>
        </dependency>
        <dependency>
            <groupId>org.treblereel.gwt.jakarta.rest</groupId>
            <artifactId>api</artifactId>
            <version>${jakarta4g.rest.version}</version>
        </dependency>
        <dependency>
            <groupId>org.treblereel.gwt.json.mapper</groupId>
            <artifactId>common</artifactId>
            <version>${mapper.json.version}</version>
        </dependency>
        <dependency>
            <groupId>org.treblereel.j2cl.processors</groupId>
            <artifactId>annotations</artifactId>
            <version>${org.treblereel.j2cl.processors.version}</version>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>io.crysknife</groupId>
                            <artifactId>crysknife-processor</artifactId>
                            <version>${crysknife.version}</version>
                        </path>
                        <path>
                            <groupId>io.crysknife.ui</groupId>
                            <artifactId>crysknife-ui-generator</artifactId>
                            <version>${crysknife.version}</version>
                        </path>
                        <path>
                            <groupId>io.crysknife.ui</groupId>
                            <artifactId>crysknife-ui-rest-generator</artifactId>
                            <version>${crysknife.version}</version>
                        </path>
                        <path>
                            <groupId>io.crysknife</groupId>
                            <artifactId>crysknife-core</artifactId>
                            <version>${crysknife.version}</version>
                        </path>
                        <path>
                            <groupId>io.crysknife.ui</groupId>
                            <artifactId>crysknife-ui-core</artifactId>
                            <version>${crysknife.version}</version>
                        </path>
                        <path>
                            <groupId>io.crysknife.ui</groupId>
                            <artifactId>crysknife-ui-rest-api</artifactId>
                            <version>${crysknife.version}</version>
                        </path>
                        <path>
                            <groupId>org.treblereel.gwt.jakarta.rest</groupId>
                            <artifactId>processor</artifactId>
                            <version>${jakarta4g.rest.version}</version>
                        </path>
                        <path>
                            <groupId>org.treblereel.gwt.json.mapper</groupId>
                            <artifactId>processor</artifactId>
                            <version>${mapper.json.version}</version>
                        </path>
                        <path>
                            <groupId>org.treblereel.j2cl.processors</groupId>
                            <artifactId>processors</artifactId>
                            <version>${org.treblereel.j2cl.processors.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.kie.j2cl.tools</groupId>
                <artifactId>j2cl-maven-plugin</artifactId>
                <version>0.23.8</version>
                <executions>
                    <execution>
                        <id>build-js</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>build</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <webappDirectory>${project.build.directory}/j2cl/launcherDir</webappDirectory>
                    <initialScriptFilename>app.js</initialScriptFilename>
                    <compilationLevel>ADVANCED</compilationLevel>
                    <annotationProcessorMode>IGNORE_MAVEN</annotationProcessorMode>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
